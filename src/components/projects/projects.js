import { Box, Typography } from '@mui/material';
import React from 'react';
import './projects.css'

function Projects() {
    return (
        <Box className='projects-box'>
            <Typography variant='h1' id='skills-title' >Projects</Typography>
        </Box>
    );
}

export default Projects;